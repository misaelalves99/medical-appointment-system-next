/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Adicione aqui outras opções que você tinha no next.config.ts
  // ex: images: { domains: ['example.com'] }, env: { API_URL: process.env.API_URL }
};

module.exports = nextConfig;
